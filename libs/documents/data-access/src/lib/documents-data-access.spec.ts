import { documentsDataAccess } from './documents-data-access';

describe('documentsDataAccess', () => {
  it('should work', () => {
    expect(documentsDataAccess()).toEqual('documents-data-access');
  });
});
